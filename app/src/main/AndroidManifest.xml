<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ParkEase">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.loading.LoadingActivity"
            android:exported="false"
            android:theme="@style/Theme.ParkEase" />

        <activity
            android:name=".ui.selection.ParkingSelectionActivity"
            android:exported="false"
            android:theme="@style/Theme.ParkEase" />

        <activity
            android:name=".ui.details.AreaDetailsActivity"
            android:exported="false"
            android:theme="@style/Theme.ParkEase" />

        <activity
            android:name=".ui.booking.BookingConfirmationActivity"
            android:exported="false"
            android:theme="@style/Theme.ParkEase" />

        <activity android:name=".ui.auth.LoginActivity"
            android:exported="false"
            android:theme="@style/Theme.ParkEase" />

        <activity android:name=".ui.auth.SignupActivity"
            android:exported="false"
            android:theme="@style/Theme.ParkEase" />

        <activity
            android:name=".ui.booking.InvoiceActivity"
            android:exported="false"
            android:theme="@style/Theme.ParkEase" />



    </application>
</manifest>
